#!/usr/bin/env python3

"""Module that provides a function that returns two concatenated strings."""


def concat(str1: str, str2: str) -> str:
    """type-annotated function add that takes a float a and a float b
    and returns their sum as a float.

    Args:
    str1: str
    str2: str

    Returns:
    str1 + str2
    """

    return str1 + str2
